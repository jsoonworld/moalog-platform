apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: moalog
  labels:
    app: loki
spec:
  type: ClusterIP
  ports:
    - port: 3100
      targetPort: http
      name: http
  selector:
    app: loki
