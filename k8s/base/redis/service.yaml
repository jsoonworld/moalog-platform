apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: moalog
  labels:
    app: redis
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      name: redis
  selector:
    app: redis
